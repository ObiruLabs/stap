{% extends "_layout" %}
{% set navPage = 'about' %}
{% block content %}

%main#main-content
    %section.section
        .container-fluid
            %h1.section--title.col-sm-6.col-sm-offset-3 Job Openings
            .row
                .col-sm-6.col-sm-offset-3
                    %ul
                        {% for entry in craft.entries.section('jobOpenings') %}
                        %li
                            %a{ href: '/jobs/{{ entry.slug }}' } {{ entry.title }}
                        {% endfor %}
{% endblock %}
