{% extends "_layout" %}
{% block content %}
%main#main-content
    %section.section
        .container-fluid
            %h1.section--title.col-sm-8.col-sm-offset-2 Resources
            .row
                .col-xs-12
                    {% for entry in craft.entries.section('resources') %}
                    %h2
                        %a{ href: '/resources/{{ entry.slug }}' } {{ entry.title }}
                    {% endfor %}
{% endblock %}
